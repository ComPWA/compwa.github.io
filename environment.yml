name: compwa-org
channels:
  - defaults
  - conda-forge
dependencies:
  - graphviz # for binder
  - juliaup
  - pip
  - python==3.8.*
  - pip:
      - |
        -c .constraints/py3.8.txt
        -e .[dev]
variables:
  PYTHONHASHSEED: 0
