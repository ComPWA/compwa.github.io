name: compwa.github.io
channels:
  - conda-forge
dependencies:
  - graphviz # for binder
  - juliaup
  - pip
  - python==3.12.*
  - pip:
      - -c .constraints/py3.12.txt -e .[dev]
variables:
  PRETTIER_LEGACY_CLI: 1
  PYTHONHASHSEED: 0
