name: pytest-notebook

on:
  push:
    branches:
      - main
      - epic/*
  pull_request:
    branches:
      - main
      - epic/*

jobs:
  pytest:
    name: Test modified notebooks
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-20.04]
        python-version: [3.7]
    steps:
      - uses: actions/checkout@master
        with:
          fetch-depth: 0
      - name: Check modified notebooks
        id: git-diff
        run:
          echo "::set-output name=modified-notebooks::$(git diff --name-only
          origin/$GITHUB_BASE_REF -- docs | grep ipynb)"
      - name: Show modified notebooks
        run: echo ${{join(steps.git-diff.outputs.*, '\n')}}
      - name: Set up Python ${{ matrix.python-version }}
        if: steps.git-diff.outputs.modified-notebooks != ''
        uses: actions/setup-python@master
        with:
          python-version: ${{ matrix.python-version }}
      - name: Install dependencies
        if: steps.git-diff.outputs.modified-notebooks != ''
        run: |
          python -m pip install --upgrade pip
          pip install -c .constraints/py3.7.txt .[test]
          sudo apt-get -y install graphviz
      - name: Run notebooks with pytest (diff only)
        if: steps.git-diff.outputs.modified-notebooks != ''
        run: pytest --nbmake ${{join(steps.git-diff.outputs.*, '\n')}}
